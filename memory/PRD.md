# SYNC 2.0 / Goorin B2B Ops – PRD

Original request: port attached Vite/TS Bolt project into CRA env as a pixel-perfect frontend app shell. Auth + data are MOCKED (localStorage stub in src/lib/supabase.ts).

## Changelog
- 2026-06: Replaced brand logo (sidebar expanded + login topbar) with `/public/b2b_ops_dark.webp` (Goorin B2B Ops). Collapsed sidebar icon unchanged.
- 2026-06: Sidebar nav → "My Account" (Dashboard, Marketplace, Pre-Book, My Orders, Shipments, Resources, Statements, Profile & Addresses) + Settings footer; global --accent changed red→green (#00d4a1).
- 2026-06: New `DashboardPage.tsx` + `dashboard.css` (greeting hero, YTD spend dark card w/ sparkline, balance & terms cards, quick actions that navigate, Recent Orders table w/ Recent/All toggle, search, sort, CSV export). Non-dashboard nav items show a "coming soon" placeholder.
- 2026-06: Login page refined: guest button & signup removed, "Need access? Contact your administrator.", grey Log in until filled, stacked footer. Mock auth: any email + password ≥6 chars.
- Legacy pages (Monitoring, Flows, AI Canvas, Connections, Runs, Activity, Field Watch) still exist in src but are no longer reachable from nav.

## Backlog
- P1: Build Marketplace, Pre-Book, My Orders, Shipments, Resources, Statements, Profile & Addresses pages
- P1: Real backend (FastAPI/Mongo) for orders, balances, auth
- P2: Update topbar (cart icon, account badge like reference), search placeholder copy
- 2026-06: Dashboard refinement — topbar cart btn + account chip (Ryan M / AS / Mirabile Distribution), account summary strip, YTD ↔ Last-12-mo spend toggle w/ monthly bars + tooltips, order detail drawer (timeline, tracking, line items, totals, Reorder/Invoice toasts), Upcoming shipments + Announcements widgets, skeleton loader, staggered reveals, rich empty state. Tested (iteration_2, 46/46 pass).
- 2026-06: Per user, removed added widgets (Announcements, Upcoming shipments) and account summary strip. Dashboard now = hero → 3 stat cards (spend bars + range toggle) → quick actions → Recent Orders (row click opens order drawer). Login: clicking Log in with empty fields signs in as demo account.
- 2026-06: Design polish pass on dashboard — hero (accent tick, gradient name, date pill, thin rule w/ black tab), count-up numbers, bars grow-in, grain/glow on dark card, balance card stacked past-due/current bar + legend, terms card Prepay/Net 60 segments, card glyphs, quick-action hover accent underline + icon tilt, tighter table rhythm w/ tinted header. Notification bell dot stays RED (#ff3048) per user — do not change.
- 2026-06: Stat card refinement — unified visual language (cards 2 & 3 both use segmented bar + legend), removed Prepay/Net60 boxes and "live data" text, slimmer chips/toggles/glyphs (34–36px), trackless bars w/ baseline + 3-letter month labels, tighter paddings (28px), quick actions 48px icons w/ 2-line clamp subtitles.
- 2026-06: Stat cards + quick actions redesigned per design_agent blueprint (/app/design_guidelines.json): flat 24px-padded cards (.stat / .stat--dark), label-top + 32px value, 42px minimal bar chart w/ 3-label axis + tooltip, short explanatory note, 2-col dt/dd meta footer (no mini bars/segments), 28px chips. Quick actions (.qa) are vertical: 42px green-tint icon, title, subtitle, CTA link. Tested iteration_3 (all pass). Note: dev tooling wraps dynamic text in <span>; never style bare `dd span` — use explicit classes.
- 2026-06: Light refinement — hero toned down (38px, no gradient/tab, 28px date pill), Recent Orders card restyled to match flat card system (18px title, 32px segmented control, 42px search/export, 16px row padding, 26px pills, uppercase 11px headers).
- 2026-06: Dashboard pass — hero subtitle + calendar date pill, stat cards min-height 250 w/ notes vertically balanced, 48px chart, 34px values, quick-action CTAs pinned to bottom, tablet layout (dark card spans full width ≤1240px). Profile menu Reliability block replaced with account summary (name/email/company/account #/rep).
- NEXT (user requested, in progress): Marketplace page per reference screenshot — toolbar (search, collections, shapes, sort, favorites, tile/list, template, import), "Showing N products / Ships from San Diego", product cards (image, TRENDING/LOW badge, heart, ORIGINAL RECIPE tag, dots, name+info, SKU, $ WHSL + MSRP, availability, Max, Add to cart). Product image asset: /tmp/a2.webp (Lone Wolf trucker) — copy to /public.
- 2026-06: Marketplace (MarketplacePage.tsx, marketplace.css, lib/products.ts, lib/cart.tsx CartProvider w/ 15-min reservation): toolbar (search/collection/shape/sort/favorites/tile-list/template/import, sticky w/ blur, active filter chips), 12 product cards w/ real cutout images (/public/products/*.webp), inline Qty stepper w/ typed qty (Qty.tsx), image lightbox (Lightbox.tsx, portal to body, 4 framed views, arrows/keys/swipe/zoom/thumbs), floating cart bar → Checkout.
- 2026-06: CheckoutPage.tsx (stepper Cart/Shipping/Payment, contact, saved/new address w/ validation, billing, delivery, custom ship window, PO, sticky summary w/ qty controls, payment terms, place order → toast → My Orders).
- 2026-06: PreBookPage.tsx (overview: dark hero + next deadline, 3 drops, how-it-works; Drop builder: toolbar, MOQ cards w/ progress, enlarge, pre-book cart panel w/ shipping/ship-to/bill-to/PO/notes/custom window, review step, submit). Shared money() in lib/money.ts (thousands separators). Tests: iteration_4 & 5 all pass.
- Profile menu Reliability block → account summary. Bell dot stays red.
- 2026-06: Dashboard mobile pass — media queries moved to end of dashboard.css (were being overridden by later rules), 1-col stats/actions on phones, quick actions become compact rows, Recent Orders table renders as stacked cards (thead hidden, grid areas w/ "Placed/Ships" labels), drawer full width, no horizontal overflow at 390px. Tablet (≤1240) dark card spans full width.
- 2026-06: Pre-Book refinement — compact builder hero (deadline / ship window / reserved units chips), MOQ "n more to confirm" helper, per-drop reservation state lifted to PreBookPage (overview shows "N units reserved" pill per drop), mobile sticky summary bar (≤1180px) that scrolls to cart, fixed cart-line stepper layout (scoped .pb-cart-text).
- Pending user asks not yet done: further Dashboard refinement (ideas: product thumbs in Items column, trend chip on YTD, active-bar value label); Marketplace refinement (ideas: SKU placeholder for no-image, margin % under price, "Sort:" label, skeleton, "/" focuses search).
- 2026-06: Built My Orders (MyOrdersPage.tsx + lib/orders.ts: scope tabs, Orders/Line Items modes, search, payment filter, sort, export, detail w/ dark hero + line items + Re-order/Sales order/Invoice), Shipments (ShipmentsPage.tsx: COO toggle, list, detail w/ stage tracker, requirements, docs, shipping instructions, chat), Resources/Statements/Profile (AccountPages.tsx: file grid w/ upload + share links, statements list w/ stats, editable contact + address book + credit terms), Settings > Team access (TeamAccess.tsx: invite, roles, member actions). Checkout contrast pass. Global premium polish layer premium.css (loaded last from index.tsx). Test iteration_6: all pass except 3 fixes applied (reorder products added p13/p14, clipboard .catch, resources mobile 1-col).
- Known: Settings 'Company preferences' & 'Notifications' tabs still contain legacy shell content (flows/retention copy) — candidates for next pass.
- 2026-06: Resources → collection folders (SS27 latest, FW26, Brand Assets, Sales Materials) with folder table view, upload + share. Settings simplified (TeamAccess + SimpleSettings toggle lists). Toasts: closable, progress bar, pause on hover. Browser-back support: App syncs activeNav with history/#hash (deep links work; activeNav initialized from hash), useBackable() closes sub-views on Back (lib/nav.ts). Shipments detail: editable Shipping Instructions + doc upload list. Dashboard cards: refined dark card, slim two-tone status bars. Mobile: topbar search hidden ≤760px, Marketplace collapsible Filters, tighter paddings across pages. Tests iteration_7/8 pass.
- 2026-06: Profile & Addresses → 2-col layout (contact + address card grid w/ 3 seeded addresses, use-type labels, default/remove actions, add tile; sticky credit terms aside). Team access: "Access levels" section removed per user.
- 2026-06: Shipment detail design refresh (stepper w/ checks, neutral requirement tiles + COMPLETE tags, accent-border status note); shipping-instructions edit form responsive fixes.
- 2026-06: Pre-Book drops list — drops listed 1→2→3; closed drops are disabled (no click/hover/chevron), compact single-row muted layout w/ lock icon + "Closed · <date>" pill; open drop keeps green ring + days-left pill. Profile "Copy account #" button removed from Credit terms.
- 2026-06: Dashboard stat cards refined (quiet chrome: no hover lift/glow, 12px uppercase labels, 36px values, terms "50% Prepay / 50% Net 60" number emphasis, 6px slim bars, money() open amount). Mobile fixes ≤760px: global `table{min-width:650px}` in index.css was forcing overflow — overridden for .ord-table/.tm-table; dashboard mobile table rules scoped to .dash-table--orders; My Orders (ord-table--orders/--lines) + Shipments (ord-table--ships) render as stacked cards via flex order + tr::after line break; Team table stacked; shipment detail reqs 1-col; pre-book closed drop compact. Test iteration_9 pass.
- 2026-06: Fixed TS2786 build error — lucide `Lock` collided with DOM global `Lock` type in PreBookPage.tsx; imported as `Lock as LockIcon`. Lesson: alias lucide icons whose names match DOM globals (Lock, Image, Text, Map, etc.).
- 2026-06: Removed mobile sidebar close (X) button per user; drawer closes via backdrop tap or nav selection.
- 2026-06: Stat cards v3 — shared row rhythm (label→value→visual→footer, footers bottom-aligned via flex:1 visual), YTD: "+18% vs LY" delta chip + $ label over active bar, taller chart; Balance: AR aging strip (Current/1–30/31–60/60+) + footer Past due / Credit available; Terms: 3-step payment timeline (Order placed 50% → Ships → Net 60 due 50%) with $ from open orders. Components AgingStrip/TermsTimeline in DashboardPage.tsx; CSS block "Stat cards v3" at end of premium.css.
- 2026-06: Pre-Book overview simplified per user ("too much"): hero + deadline card (green), drops 1→3 with closed rows compact ("Window closed"), open drop has "Reserve units" CTA; season timeline/chips/MOQ block removed. Shipment detail v2: dark hero (id, status, 3 stat tiles, 7-stage rail "Stage N of 7"), main column = Release requirements checklist, Line items (100 mock lines for open shipments via shipLines(), 520px scroll area w/ sticky header + sticky "Declared value" footer), conversation w/ avatar; right column (static, not sticky — sticky caused visual gap) = Documents w/ sizes + Download all, Shipping instructions (editable), COO toggle. Shipments list/export use shipTotal(). Test iteration_10 pass (before simplification + 100-line table; those self-tested via screenshots).
- 2026-06: Payment terms card simplified: value "50 / 50 · Net 60", one-line note, two-segment split bar (Prepaid at order / Due Net 60 with $), footer unchanged. TermsTimeline removed (TermsSplit component).
- 2026-06: Terms card legend restyled to match aging strip (uppercase small labels w/ color dot + bold amounts, right item right-aligned).
- 2026-06: Terms card redesigned as schedule rows (TermsSchedule): value "Net 60", note "Split 50/50 across N open orders", two rows [50% badge · Prepay at order · $ · PAID] / [50% · Net 60 after shipment · $ · DUE]. CSS .stat-sched in premium.css.
- 2026-06: Terms row label changed to "Prepay before order ships" per user (nowrap desktop, wraps on mobile).
- 2026-06: Third dashboard card replaced: "Payments due" — derived schedule from open orders (prepay due 7d before ship if Not invoiced; Net 60 due shipDate+60 if not Paid), value = total due, 7-column month bars (current month + 5 + Later), footer Next payment / Due in 30 days. paymentsDue()/DueByMonth in DashboardPage.tsx, .stat-due CSS in premium.css. Shipping instructions booking method: "Goorin arranges" option removed.
- 2026-06: Searchable CountrySelect combobox (src/CountrySelect.tsx: button + popover w/ search, keyboard nav; testids <id>, <id>-search, <id>-opt-<slug>) used in shipping instructions, checkout address, profile address form. Dashboard card 3 = "Invoiced payments due": month-bucket rows (badge, Month YYYY, n invoices · SO refs, amount) + footer Next payment / Due in 30 days. Stat footers now a tinted full-bleed band with vertical divider. Test iteration_11.
- 2026-06: Design cohesion layer (end of premium.css, "DESIGN SYSTEM"): H1 28 / dark-hero 30 / H2 17 / caps labels 11 / body 14; primary ink buttons (co-primary 48, mk-add 42), secondary outlined 42 radius 12, text links ink (no blue), segments 36, pills 26, inputs 44. Account # removed from topbar menu + Profile credit card (Sales rep tile instead). Sidebar: "Dashboard" renamed "Overview" (hash #overview), green live-dot removed. Resources cards keep original design + solid folder tab cue on cover (rs-cover::before/::after). Test iteration_12 pass.
- 2026-06: Statements page refined: 3 stat cards (open balance w/ chip, paid YTD, next payment due $8.50 · Oct 24), year segment (2026/2025/All) + Download all, search + summary, table (Statement/Status/Invoices/Charges/Payments/Balance/PDF) w/ mobile card layout (.ord-table--stmts), empty state. Resources folder cover reverted to reference look (dark gradient, outlined tab, no product image).
- 2026-06: Account payment-terms mode (lib/account.ts useTerms(): invoiced | card, localStorage "goorin.terms", switch in Profile › Credit terms tile, testids terms-invoiced/terms-card). Dashboard card 3 switches: invoiced → "Invoiced payments due" month buckets; card → "Upcoming card charges" (CardPaymentsCard: card-on-file Visa •••• 4242, next 3 charges before ship, footer Next charge / Charged YTD).
- 2026-06: Default terms mode set to "card" (lib/account.ts) so credit-card view shows by default; Invoiced still selectable in Profile.
- 2026-06: Terms-account dashboard polish: chart + aging visuals now flex to fill card height (cards stay level with long due lists); aging strip is proportional bars (green current / amber 1–30 / red 31–60, 60+), past-due chip turns red (stat-chip--bad) when pastDue>0 (aging tuple in DashboardPage, currently [0,0,0]); due-month list shows all months in a 214px scroll area.
- 2026-06: Balance card aging redesigned: stacked proportional bar (green/amber/red) + 2×2 tiles (label w/ dot, amount, share %); "action required" note turns red; due-month list fixed 152px 3-row window + "Scroll for N more months" caption; compact() handles $0/$k/$M.
- 2026-06: Checkout: top row = pill Back · centered steps · "Step N of 3"; new-address form is a card (co-form-card) with header, Save address (validates name/line1/city/state/zip, adds to address list + selects it) and Cancel; addresses state list. Pre-Book builder topline: pill back button, centered steps, green "Closes Sep 21" chip. Marketplace cart bar portaled to body (page-in animation transform on .page-content > * was breaking position:fixed).
- 2026-06: Checkout address Edit now works (prefilled card form, "Save changes" updates in place; editingIdx state); sidebar Marketplace highlighted while on Checkout. Test iteration_13.
- 2026-06: Resources: folder cards = clean dark cover (icon tile, title bottom-left, LATEST, file count); folder view = dark hero (eyebrow, name, meta: files · size · updated) + aligned toolbar (filter, Download all, green Upload), table w/ Type pills, filename+ext, mono sizes, icon actions. All folder-tab/clip-path experiments removed.
- 2026-06: Overview hero date → calendar block (icon tile, FRIDAY / September 11 / 2026) vertically centered with heading (dash-date--block). Resources back button restyled to shared pill.
- 2026-06: Checkout address form renders inline: edit form attached under the edited card (co-address-slot.is-editing joined styling); new-address form under the Add tile. Overview date simplified to inline "FRIDAY · September 11"; Pre-Book open drop divider removed.
- 2026-06: Stat cards variant B active: SpendArea (SVG smooth area sparkline, end dot + $ label), AgingRing (donut + legend, HTML center %), DueTimeline (vertical timeline of due months, first = next highlighted). Previous components (SpendBars, AgingStrip, DueByMonth) still in DashboardPage.tsx for easy switch-back.
- 2026-06: Balance card: 112px seamless donut (gaps only when >1 segment), legend rows w/ dividers + share %, "Credit used" utilization bar (limit 5000, stat-credit-usage).
- 2026-06: Balance card variant C active: AgingBars (horizontal proportional bars per bucket + amounts) + credit-used bar. AgingRing/AgingStrip remain available.
- 2026-06: Balance card = 5 uniform rows (4 aging buckets + dashed-divider "Credit used" row) with amount + share %; empty buckets use dashed tracks. Due timeline rows show "N invoices · SO refs" and relative timing ("in 42d", "in 4 mo") instead of bars.
- 2026-06: Stat cards "simple" variant active: AgingSimple (slim stacked bar + 4-row hairline legend) and DueSimple (hairline list: month, "n invoices · in Xd", amount). Other variants kept in file.
- 2026-06: Stat cards variant D active: AgingFigures (Current / Past due figure blocks w/ accent border + credit-used line) and DueTiles (month tiles, next = dark; scrolls if >3). Other variants kept.
- 2026-06: Stat cards variant E active: CreditGauge (half-arc credit utilization, Current/Past due tiles) + DueRunway (7-month runway w/ today marker and amount bars at due months).
- 2026-06: Stat cards variant F active: CreditLine (3 columns + credit-line bar) + DueDataBars (data-bar rows).
- 2026-06: Settings page v2: hero (title + "Changes save automatically" chip), 2-col layout — left: dark account card, vertical nav (icon tile, label, hint, chevron; testids settings-tab-users/workspace/notifications), link to Profile; right: section head + content (TeamAccess / SimpleSettings). Mobile: nav becomes horizontal scroll.
- 2026-06: Settings mobile: 3-tile icon nav (Team / Company / Alerts short labels via tab.short), compact account card, autosave chip left-aligned, 2×2→4-col compact team stats. Fixed grid min-width bug with minmax(0,1fr).
- 2026-06: Pre-Book hero v3: compact, facts line (1 drop open · 2 closed · Ships …), deadline panel = label + days-left chip, big date, countdown bar, "Order by". Mobile: hero stacks, open drop card keeps icon+title row, full-width CTA, 2-col meta.
- 2026-06: Pre-Book mobile hero condensed (lede hidden, compact deadline row).
- 2026-06: Drop builder hero v3: compact, "Drop 3 · OPEN" tag, facts strip (Order by w/ time + days left, Ships, Reserved units + styles) as joined dl (pb-facts); mobile 2+1 grid, lede hidden.
- 2026-06: Drop builder mobile: compact toolbar (search row + view toggle/Template/Import in one row), 2-col product grid with smaller type/controls, cart panel static.
- 2026-06: Stat cards variant G active: CreditMeter (Current/Past due tiles + tick-marked credit meter with marker + note) and DueCalendar (calendar-tile rows OCT/24 etc.).
- 2026-06: Card-terms accounts: middle card = "Open orders" (OpenOrdersCard: value, units chip, In production / Ready to ship tiles, fulfillment progress meter Placed→Shipped, next ship date, footer Orders YTD / Average order) replacing Outstanding balance (no credit line for card customers).
- 2026-06 (RESTORE): Restored the full Goorin B2B Ops project from uploaded zip into the workspace, as-is (mocked auth + data). Copied frontend/src, package.json, craco/tailwind/tsconfig configs, public assets, backend, memory; preserved .env files. Removed stale jsconfig.json (TS project), cleared craco cache to fix a stale index.js entry-resolution error. yarn install + pip install done, services running, frontend compiles clean. Verified end-to-end by testing agent (iteration_14): login (demo empty + typed creds), all 8 nav pages, Marketplace add-to-cart, Checkout, Settings — 100% pass, no console errors.
- 2026-06: OpenOrdersCard reworked: no fabricated production stages
- 2026-06 (DASHBOARD REDESIGN): Rebuilt DashboardPage.tsx as a sales-ops revenue view per reference screenshots — dark hero (YTD invoiced $ + red delta chip, All/US Wholesale/Distributors segment toggle that swaps figures + chart, 3 tiles Open/Total/Forecast, Annual Goal Progress bar with pace marker + Behind-pace badge), Revenue-by-Month recharts ComposedChart (stacked Invoiced+Open bars, Total line, dashed Forecast line, custom tooltip, Export CSV), Segments panel (US Wholesale/Distributors progress + Top Accounts list w/ YoY), and expandable Account Detail table (Invoiced mini-bar, vs Goal, YoY, row expand → 5 metric tiles + Open account, search). New rv-* classes in dashboard.css. Verified iteration_16 (100%).
- 2026-06 (DASHBOARD NEXT-LEVEL): Rewrote DashboardPage.tsx + dashboard.css to a modern premium look — hero with layered dual glow + faint grid, eyebrow w/ live dot, gradient-clipped figure, 9-month invoiced sparkline (recharts AreaChart), sliding-indicator segment control, tiles with mini spark-bars, glowing animated goal bar; chart gained a gradient Area under the Total line, dashed grid, and a YTD/12M/Q range toggle (slices months); Segments card now uses conic-gradient % rings; account table shows initials avatars + vs-goal pills; account count reflects active search ("N of 220"). Verified iteration_18 (100%, 0 console errors). Added a refinement layer to dashboard.css and prebookreview.css — staggered fade-up entrance for hero/cards/panels, hover lift + deeper shadows on cards & metric/segment tiles, animated goal/segment/MOQ/fill bars (scaleX grow-in), pop-in scale animation for Actions/More/row menus, hover indent on menu items, focus-visible rings, focus glow on search/inputs, amber left-accent on the purchasing-preview banner, inner top-highlight on dark surfaces, and a prefers-reduced-motion guard. Compiles clean. ROOT CAUSE of the center-stacked header + wrapping sidebar was a CSS class-name collision — the legacy `prebook.css` (still imported globally by ShipmentsPage/MyOrdersPage) defines bare `.pb-deadline{flex-direction:column;align-items:center;padding}`, `.pb-drop-head>div{flex-direction:column;flex:1}`, `.pb-drop-title`, `.pb-table`, `.pb-moq`, `.pb-season`, `.pb-drops` that overrode the new Pre-Book Review page. Fixed by renaming those 7 colliding classes in PreBookPage.tsx + prebookreview.css to a unique `pbx-*` namespace (no shared-CSS bleed). Also restructured header markup: left-aligned row — status dot + "SS27 / Drop 3" (grey "/ Drop 3") with inline OPEN pill, "Deadline 9/21/2026" beneath, bottom divider; season sidebar card with nowrap drop labels/badges. Compiles clean. Upgraded header (functional "…" More menu: Edit/Duplicate/Export/Archive), Actions menu now selection-aware (order checkboxes drive a count badge + "Release selected" vs "Release all confirmed"; Freeze/Cancel/Restore), Escape/backdrop close all popovers. Purchasing-preview banner fully expandable (channel-not-configured note, Consolidation-details accordion → per-order table, disabled Freeze button, legend). Orders table: working row checkboxes + select-all, row expand → line items, per-row "…" menu (View/Release/Cancel). SKU Demand rows expand → channel breakdown grid + gap-to-MOQ. SETUP tab now interactive: drops CRUD (New drop, inline edit form for MOQ policy + USW/Distributor windows, Save, Remove), Product Groups CRUD (Add group, editable customer-group chips add/remove, remove group), Collections New Pre-Book. New CSS in prebookreview.css. Verified iteration_17 (100%, 0 console errors).
- 2026-06 (NAV UPDATE): Restructured the left sidebar to 3 grouped sections + footer. ANALYTICS (Dashboard, Analytics, Open Orders, Invoices, Allocations, Aging AR); MARKETPLACE (Customers, Compliance, Credit Terms, Onboarding, Marketplace, Marketing Assets); TOOLS (Forecast Goals, Pre-Book Review, Place Order, ATS Inventory, Intl Shipments, Commissions, Feedback); footer (Settings, Operational Health). Wired existing pages to new labels: Dashboard→DashboardPage, Open Orders→MyOrdersPage, Invoices→StatementsPage, Credit Terms→ProfilePage, Marketplace→MarketplacePage, Marketing Assets→ResourcesPage, Pre-Book Review→PreBookPage, Intl Shipments→ShipmentsPage. Remaining 12 items show the existing "coming soon" placeholder. Updated internal nav targets (Dashboard quick actions, OrdersTable view-all, Pre-Book submit, Checkout complete). Verified iteration_15 (100% frontend pass). — list of open orders (id, est. ship date, Charged / Awaiting charge tag from payment status, amount); note = "$34 charged · $17 to be charged".


## 2026-06 Dashboard refinement (iteration_19 PASS)
- Segments: replaced rings with '% of goal' pills, invoiced/goal text, pace marker on bar
- Top accounts: ranked #1-5 with triangle YoY deltas
- Chart: cursor line, active dots, larger tooltip, 'Open Orders' legend, conditional Sep reference line
- Chart card and Segments card now equal height (grid stretch, chart fills, iteration_20 PASS)
- 2026-06 Full dashboard redesign: Heritage Emerald/Obsidian palette, Barlow Condensed/Outfit/JetBrains Mono fonts, 2-col hero with vertical KPI stack, kickers on cards, mono numerics (iteration_21 PASS). Design spec at /app/design_guidelines.json
- Redesign retry: dropped Barlow Condensed + JetBrains Mono (too harsh) → Outfit headings, DM Sans tabular numerals, glass KPI tiles in hero (screenshot-verified)
- 2026-06 Dashboard redesign v4 (bento): mint hero card w/ area chart + segment pills, goal ring card, KPI icon-chip cards, chart+segments(rings)+top accounts, account table. Airy green/navy/amber palette, Outfit+DM Sans. iteration_23.
- Refinement: hero stats strip (Prior YTD/Avg/Best month), segment pts-vs-pace, zebra rows; removed decorative KPI spark bars → labeled meter bars (0f pipeline / 0f goal). iteration_24/25.
- Refinement 2: true % on KPI meters, fixed-width invoiced progress track in table, rank chips, lowercase labels. iteration_26.
- KPI cards v3: goal ring w/ % inside + pace tick + remaining; KPI cards show stacked composition bars (ship month / invoiced-open / forecast vs goal). iteration_27.
- 2026-06 Pre-Book Review restyled to bento look: light stat cards w/ MOQ %, side-by-side status cards, days-left chip, pill controls, zebra table. iteration_28.
- Consolidation pass: hero now includes Annual goal side panel; 3 KPIs merged into one strip card (5 cards → 2). iteration_29.
- Segments card removed; channel goal rows integrated into hero goal panel; right column = Top accounts (8). iteration_30.
- Simplification (b): removed hero stats strip, KPI bars/legends, TODAY line + Forecast series, Top accounts card; chart full width; Accounts table ranked with Top 1-5 pins. iteration_31.
- Pre-Book refine 2: stat strip card, filter counts, Under gap pills, sticky header/sidebar. iteration_32.
- Pre-Book pass 3: themed custom checkbox (Show archived), mint hero header w/ kicker + deadline progress track, 'Close' MOQ state (>=75%), season drop progress, entrance animation. iteration_33.
- Final polish pass both pages: removed zebra, single pill per account row, white status cards w/ icon chips on Pre-Book, unified paddings. iteration_34.
- Pre-Book pass 4: 3 stat cards → one Drop health card (4 metrics + channel mix bar); SKU rows sorted by fill% desc; totals row. iteration_35.
- Pre-Book pass 5 (structural): 3-column layout — seasons | SKU table (primary) | sticky rail (Ready, Purchasing preview, Drop health). Status col → Gap pill. iteration_36.
- Pre-Book v6 (Review tab): sidebar removed → top drop switcher strip; full-width hero (title, deadline, status chips, stats + channel mix, actions); collapsible purchasing preview; full-width table. Setup tab keeps sidebar. iteration_37.
- Pre-Book header unified: switcher band moved inside hero card (top band), Review/Setup as underline text tabs. iteration_38.
- Polish: SKU Demand tab count pill, orders header alignment, dashboard expand spacing. iteration_39.
- Multi-season support: SEASONS data (SS27, FW27, archived FW26/SS26), season picker menu on pill, drops/hero derive from active season; Show archived filters picker. iteration_40.

## 2026-06 Intl Shipments page (NEW, MOCKED)
- /app/frontend/src/IntlShipmentsPage.tsx + intlshipments.css (is-* prefix), wired in App.tsx for 'Intl Shipments'
- List: priority strip, KPIs, search + status/customer/factory filters, pipeline table, footer totals, New Shipment modal (mock create)
- Detail: stepper, advance-status CTA, more menu, customer/factory facts, booking & prepayment, conversation (send), packing list, documents, lines table, activity feed
- Intl Shipments detail overhaul: hero (route, 6 facts, segmented progress, next-step hint) + tabs Overview / Booking & payment / Conversation / Documents / Activity. Summary rows show incoterms+mode. iteration_42.
- Intl refinements: ship-date chips (soon/overdue/shipped) in list + hero, prepayment awaiting/received label, chat empty state, click-to-copy emails. iteration_43.
- Intl next-level: Open Orders tab (create shipment prefilled from order), Factories drawer (contacts, incoterms, lead time, new shipment here), mock booking document upload (listed in Booking + Documents, logged in activity). iteration_44.
- Intl polish: KPI cells act as quick filters, attention chips show reason, search clear button, row hover chevron. iteration_45.

## 2026-06 INTL Shipments + Pre-Book sidebar pass (iteration_47/48 PASS)
- Open Orders tab rebuilt as PO-level planner (`IntlOpenOrders.tsx`): filters, "Within N days" window, Recommended order groups band, sortable table, expandable SKU lines (Ordered/Allocated/Shipped/Remaining), create shipment per PO / per group.
- Shipments tab: page header + segmented switcher w/ counts, stage-filter chips (replaces status select), rows with monogram + 7-seg progress, Needs-attention card grouped by reason (top 3 + "show all") scaling to many blocked shipments.
- Pre-Book Review: left sidebar season tree restored (collapsible seasons → drops, new drop, show archived in footer); Setup collections list restyled. Hero season pill/drop tabs removed.
- Backlog: detail-view stepper polish + "next action" checklist card; real backend; prebook.css cleanup.
